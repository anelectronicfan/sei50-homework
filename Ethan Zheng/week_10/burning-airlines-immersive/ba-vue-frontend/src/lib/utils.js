export const adder = (first, second) => {
  return first + second;
}

export const subber = (first, second) => {
  return first - second;
}

export const bigNum = 1231231231151234;

export default adder;